name: test_task_solid
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1


dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  solid_lints: ^0.3.1

flutter:
  uses-material-design: true